<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">


  <!-- Managing both DB that use sequences and db that use auto increment -->
  <property name="autoIncrement" value="true" dbms="mysql,mssql,h2,sybase,db2,hsqldb"/>
  <property name="autoIncrement" value="false" dbms="oracle,postgresql"/>
  
  <changeSet author="email-connector" id="1.0.0-0" dbms="oracle,postgresql,hsqldb">
    <createSequence sequenceName="SEQ_EMAIL_CONNECTOR_ID" startValue="1"/>
  </changeSet>

  <changeSet author="email-connector" id="1.0.0-1">
    <createTable tableName="EMAIL_CONNECTOR">
      <column name="ID" type="BIGINT" autoIncrement="${autoIncrement}" startWith="1">
        <constraints nullable="false" primaryKey="true" primaryKeyName="PK_EMAIL_CONNECTOR"/>
      </column>
      <column name="NAME" type="NVARCHAR(250)">
        <constraints nullable="false"/>
      </column>
      <column name="IMAGE_FILE_ID" type="BIGINT">
      </column>
      <column name="ICON" type="NVARCHAR(500)">
      </column>
      <column name="IMAP_URL" type="NVARCHAR(250)">
        <constraints nullable="false"/>
      </column>
      <column name="PORT" type="NVARCHAR(50)">
        <constraints nullable="false"/>
      </column>
      <column name="ACTIVE" type="BOOLEAN" defaultValueBoolean="true">
        <constraints nullable="false" />
      </column>
    </createTable>
    <modifySql dbms="mysql">
      <append value=" ENGINE=INNODB CHARSET=UTF8 COLLATE utf8_general_ci"/>
    </modifySql>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-2">
    <createTable tableName="EMAIL_BOX">
      <column name="ID" type="BIGINT" autoIncrement="${autoIncrement}" startWith="1">
        <constraints nullable="false" primaryKey="true" primaryKeyName="PK_EMAIL_BOX"/>
      </column>
      <column name="MAIL_REMOTE_ID" type="BIGINT">
        <constraints nullable="false"/>
      </column>
      <column name="USER_ID" type="VARCHAR(250)">
        <constraints nullable="false"/>
      </column>
      <column name="SUBJECT" type="VARCHAR(1024)">
      </column>
      <column name="EXCERPT" type="VARCHAR(1024)">
      </column>
      <column name="SENDER" type="VARCHAR(250)">
        <constraints nullable="false"/>
      </column>
      <column name="SENT_DATE" type="TIMESTAMP">
        <constraints nullable="false" />
      </column>
    </createTable>
    <modifySql dbms="mysql">
      <append value=" ENGINE=INNODB CHARSET=UTF8MB4 COLLATE utf8mb4_0900_ai_ci" />
    </modifySql>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-3" dbms="oracle,postgresql,hsqldb">
    <createSequence sequenceName="SEQ_EMAIL_ID" startValue="1"/>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-4">
    <addColumn tableName="EMAIL_BOX">
      <column name="IS_READ" type="BOOLEAN" defaultValueBoolean="false"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-5">
    <validCheckSum>ANY</validCheckSum>
    <createTable tableName="EMAIL_ATTACHMENTS">
      <column name="ID" type="BIGINT" autoIncrement="${autoIncrement}" startWith="1">
        <constraints nullable="false" primaryKey="true" primaryKeyName="PK_EMAIL_ATTACHMENTS"/>
      </column>
      <column name="MAIL_ID" type="BIGINT">
        <constraints nullable="false"/>
      </column>
      <column name="ATTACHMENT_REMOTE_ID" type="VARCHAR(250)">
        <constraints nullable="false"/>
      </column>
      <column name="NAME" type="VARCHAR(1024)">
        <constraints nullable="false"/>
      </column>
      <column name="MIME_TYPE" type="VARCHAR(250)">
        <constraints nullable="false" />
      </column>
    </createTable>
    <modifySql dbms="mysql">
      <append value=" ENGINE=INNODB CHARSET=UTF8MB4 COLLATE utf8mb4_0900_ai_ci" />
    </modifySql>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-6">
    <addForeignKeyConstraint 
      baseTableName="EMAIL_ATTACHMENTS"
      baseColumnNames="MAIL_ID"
      referencedTableName="EMAIL_BOX"
      referencedColumnNames="ID"
      constraintName="FK_EMAIL_ATTACHMENTS_MAIL_ID"
      onDelete="CASCADE"/>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-7">
    <renameColumn
      tableName="EMAIL_BOX"
      oldColumnName="SENT_DATE"
      newColumnName="RECIEVED_DATE"
      columnDataType="TIMESTAMP" />
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-8">
    <renameColumn
      tableName="EMAIL_BOX"
      oldColumnName="EXCERPT"
      newColumnName="BODY"
      columnDataType="LONGTEXT" />
    <addColumn tableName="EMAIL_BOX">
      <column name="RECIEVER" type="VARCHAR(1024)"/>
      <column name="CC" type="VARCHAR(1024)"/>
      <column name="BCC" type="VARCHAR(1024)"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-9">
     <modifyDataType 
      tableName="EMAIL_BOX" 
      columnName="RECIEVER" 
      newDataType="LONGTEXT" />
     <modifyDataType 
      tableName="EMAIL_BOX" 
      columnName="CC" 
      newDataType="LONGTEXT" />
     <modifyDataType 
      tableName="EMAIL_BOX" 
      columnName="BCC" 
      newDataType="LONGTEXT" />
    <renameColumn
      tableName="EMAIL_BOX"
      oldColumnName="RECIEVED_DATE"
      newColumnName="RECEIVED_DATE" 
      columnDataType="TIMESTAMP" />
    <renameColumn
      tableName="EMAIL_BOX"
      oldColumnName="RECIEVER"
      newColumnName="RECEIVER"
      columnDataType="LONGTEXT" />
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-10">
    <renameColumn
      tableName="EMAIL_CONNECTOR"
      oldColumnName="PORT"
      newColumnName="IMAP_PORT" 
      columnDataType="NVARCHAR(50)" />
    <addColumn tableName="EMAIL_CONNECTOR">
      <column name="SMTP_URL" type="NVARCHAR(250)"/>
      <column name="SMTP_PORT" type="NVARCHAR(50)"/>
      <column name="SMTP_SECURITY_TYPE" type="NVARCHAR(50)"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-11">
    <addColumn tableName="EMAIL_BOX">
      <column name="MAIL_HEADER_ID" type="VARCHAR(1024)"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-12" dbms="hsqldb">
    <modifyDataType tableName="EMAIL_BOX" columnName="BODY" newDataType="CLOB"/>
    <modifyDataType tableName="EMAIL_BOX" columnName="RECEIVER" newDataType="CLOB"/>
    <modifyDataType tableName="EMAIL_BOX" columnName="CC" newDataType="CLOB"/>
    <modifyDataType tableName="EMAIL_BOX" columnName="BCC" newDataType="CLOB"/>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-13">
    <addColumn tableName="EMAIL_BOX">
      <column name="RECENT" type="BOOLEAN" defaultValueBoolean="false"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-14">
    <addColumn tableName="EMAIL_BOX">
      <column name="REPLY_TO" type="VARCHAR(1024)"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-15">
     <modifyDataType 
      tableName="EMAIL_BOX" 
      columnName="SUBJECT" 
      newDataType="TEXT" />
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-16" dbms="postgresql">
    <modifyDataType 
      tableName="EMAIL_BOX" 
      columnName="BODY" 
      newDataType="TEXT" />
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-17">
    <addColumn tableName="EMAIL_CONNECTOR">
      <column name="WEB_MAIL_URL" type="NVARCHAR(250)"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-18">
    <addColumn tableName="EMAIL_BOX">
      <!-- THREAD_ID stays at 255 so (USER_ID, THREAD_ID) fits a MySQL index key; a
           Message-ID is practically well under that. IN_REPLY_TO holds one id;
           MAIL_REFERENCES can carry a long chain, hence TEXT (as SUBJECT in 1.0.0-15). -->
      <column name="THREAD_ID" type="VARCHAR(255)"/>
      <column name="IN_REPLY_TO" type="VARCHAR(1024)"/>
      <column name="MAIL_REFERENCES" type="TEXT"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-19">
    <createIndex tableName="EMAIL_BOX" indexName="IDX_EMAIL_BOX_USER_ID">
      <column name="USER_ID"/>
    </createIndex>
    <createIndex tableName="EMAIL_BOX" indexName="IDX_EMAIL_BOX_USER_THREAD">
      <column name="USER_ID"/>
      <column name="THREAD_ID"/>
    </createIndex>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-20">
    <!-- Which remote folder a cached message belongs to (INBOX / SENT / ARCHIVE).
         IMAP UIDs are per-folder, so the cache key becomes (USER_ID, FOLDER,
         MAIL_REMOTE_ID). Existing rows are all INBOX. -->
    <addColumn tableName="EMAIL_BOX">
      <column name="FOLDER" type="VARCHAR(50)" defaultValue="INBOX">
        <constraints nullable="false"/>
      </column>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-21">
    <!-- Exchange Thread-Index conversation-root GUID (hex, 32 chars). Lets a reply
         join its conversation even when the References chain is broken (subject
         changes, external forwards). Indexed with USER_ID for the sibling lookup. -->
    <addColumn tableName="EMAIL_BOX">
      <column name="THREAD_INDEX_ROOT" type="VARCHAR(64)"/>
    </addColumn>
    <createIndex tableName="EMAIL_BOX" indexName="IDX_EMAIL_BOX_USER_THREAD_INDEX">
      <column name="USER_ID"/>
      <column name="THREAD_INDEX_ROOT"/>
    </createIndex>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-22">
    <!-- Whether the message was machine-sent, decided from the standard bulk headers at sync
         time (List-Unsubscribe, Auto-Submitted, Precedence) while the MimeMessage is still at
         hand. Consumers only see the cached row, so without this the only way to guess is to
         search the body for the word "unsubscribe", which also matches signatures, mailing
         list footers and quoted replies, and so mislabels genuine human mail. Existing rows
         default to false and are corrected on their next sync. -->
    <addColumn tableName="EMAIL_BOX">
      <column name="BULK_MAIL" type="BOOLEAN" defaultValueBoolean="false">
        <constraints nullable="false"/>
      </column>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-23">
    <!-- Replace the single BULK_MAIL flag with the raw headers it was collapsing. They do not
         mean the same thing: Auto-Submitted and Precedence say "no human wrote this", while
         List-Unsubscribe only says "this travelled through bulk distribution machinery" - a
         mailing list stamps it on every message it relays, including one a colleague typed by
         hand. Collapsing them made list traffic indistinguishable from marketing and filed
         genuine business mail as automated. List-Id plus a postable List-Post is what actually
         identifies a mailing list, since marketing senders rarely set List-Post.
         Kept as separate facts rather than a derived verdict so the classification rule can be
         changed without re-downloading every mailbox. -->
    <dropColumn tableName="EMAIL_BOX" columnName="BULK_MAIL"/>
    <addColumn tableName="EMAIL_BOX">
      <column name="AUTO_SUBMITTED" type="BOOLEAN" defaultValueBoolean="false">
        <constraints nullable="false"/>
      </column>
      <column name="HAS_LIST_ID" type="BOOLEAN" defaultValueBoolean="false">
        <constraints nullable="false"/>
      </column>
      <column name="HAS_LIST_POST" type="BOOLEAN" defaultValueBoolean="false">
        <constraints nullable="false"/>
      </column>
      <column name="HAS_LIST_UNSUBSCRIBE" type="BOOLEAN" defaultValueBoolean="false">
        <constraints nullable="false"/>
      </column>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-24">
    <!-- Who actually wrote a message that arrived through a mailing list. Google Groups and
         Mailman rewrite From to the list itself ("Name via the-list <the-list@example.com>"),
         so the stored sender identifies the transport, not the author - and a colleague's
         message then looks exactly like a newsletter relayed into the same group. The original
         address survives in X-Original-Sender; without it, anything list-borne is
         indistinguishable. Empty for directly-delivered mail. -->
    <addColumn tableName="EMAIL_BOX">
      <column name="ORIGINAL_SENDER" type="VARCHAR(320)"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-25">
    <!-- Local mirror of the IMAP Flagged flag (the "star"). The flag on the server is the
         source of truth: the sync overwrites this column with whatever the server says, so
         a star set in any other mail client shows here and a star set here is pushed back.
         Existing rows default to false and are corrected on their next sync. -->
    <addColumn tableName="EMAIL_BOX">
      <column name="STARRED" type="BOOLEAN" defaultValueBoolean="false">
        <constraints nullable="false"/>
      </column>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-26" dbms="oracle,postgresql,hsqldb">
    <createSequence sequenceName="SEQ_EMAIL_CONTACT_ID" startValue="1"/>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-27">
    <!-- The per-user contact store. Rows are collected from the user's own cached mail
         (SOURCE=COLLECTED), typed by hand (MANUAL), or synced from a CardDAV address
         book (CARDDAV, phase 3). The lowercased PRIMARY_EMAIL with USER_ID is the
         natural key: the same person arriving from several sources resolves in place
         on the one row. SUPPRESSED is the tombstone for "remove a collected contact":
         a hard delete would only see the row re-collected on the next mail from that
         person. SORT_BUCKET is the alphabetical bucket (0..25 = A..Z of the
         diacritic-stripped sort key, 26 = everything else): ordering by an integer
         keeps the letter buckets contiguous on every dialect, where ordering
         punctuation and non-Latin scripts against letters is collation-dependent.
         EMAILS and PHONES are semicolon-joined value lists (the EMAIL_BOX recipients
         precedent) and stay VARCHAR so they remain LIKE-searchable on HSQLDB.
         HREF / ETAG / VCARD_UID / CONNECTOR_ID / PHOTO_FILE_ID / ORGANIZATION / TITLE
         are the CardDAV bookkeeping seam, null until phase 3 fills them. -->
    <createTable tableName="EMAIL_CONTACT">
      <column name="ID" type="BIGINT" autoIncrement="${autoIncrement}" startWith="1">
        <constraints nullable="false" primaryKey="true" primaryKeyName="PK_EMAIL_CONTACT"/>
      </column>
      <column name="USER_ID" type="VARCHAR(250)">
        <constraints nullable="false"/>
      </column>
      <column name="SOURCE" type="VARCHAR(20)">
        <constraints nullable="false"/>
      </column>
      <column name="PRIMARY_EMAIL" type="VARCHAR(320)">
        <constraints nullable="false"/>
      </column>
      <column name="EMAILS" type="VARCHAR(2000)"/>
      <column name="DISPLAY_NAME" type="NVARCHAR(500)"/>
      <column name="GIVEN_NAME" type="NVARCHAR(250)"/>
      <column name="FAMILY_NAME" type="NVARCHAR(250)"/>
      <column name="SORT_NAME" type="NVARCHAR(255)"/>
      <column name="SORT_BUCKET" type="INT" defaultValueNumeric="26">
        <constraints nullable="false"/>
      </column>
      <column name="PHONES" type="VARCHAR(500)"/>
      <column name="ORGANIZATION" type="NVARCHAR(250)"/>
      <column name="TITLE" type="NVARCHAR(250)"/>
      <column name="PHOTO_FILE_ID" type="BIGINT"/>
      <column name="HREF" type="VARCHAR(1000)"/>
      <column name="ETAG" type="VARCHAR(250)"/>
      <column name="VCARD_UID" type="VARCHAR(250)"/>
      <column name="CONNECTOR_ID" type="BIGINT"/>
      <column name="SUPPRESSED" type="BOOLEAN" defaultValueBoolean="false">
        <constraints nullable="false"/>
      </column>
      <column name="SEEN_COUNT" type="BIGINT" defaultValueNumeric="0">
        <constraints nullable="false"/>
      </column>
      <column name="LAST_SEEN_DATE" type="TIMESTAMP"/>
      <column name="CREATED_DATE" type="TIMESTAMP"/>
      <column name="UPDATED_DATE" type="TIMESTAMP"/>
    </createTable>
    <modifySql dbms="mysql">
      <append value=" ENGINE=INNODB CHARSET=UTF8MB4 COLLATE utf8mb4_0900_ai_ci" />
    </modifySql>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-28">
    <createIndex tableName="EMAIL_CONTACT" indexName="IDX_EMAIL_CONTACT_USER_ID">
      <column name="USER_ID"/>
    </createIndex>
    <createIndex tableName="EMAIL_CONTACT" indexName="UQ_EMAIL_CONTACT_USER_EMAIL" unique="true">
      <column name="USER_ID"/>
      <column name="PRIMARY_EMAIL"/>
    </createIndex>
    <createIndex tableName="EMAIL_CONTACT" indexName="IDX_EMAIL_CONTACT_USER_SORT">
      <column name="USER_ID"/>
      <column name="SORT_BUCKET"/>
      <column name="SORT_NAME"/>
    </createIndex>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-29">
    <!-- The platform identity a DIRECTORY row links to. Imported colleagues are
         stored as a LINK (this username), not a copy: name, avatar and address
         resolve live from the profile at read time, so a rename or a departure
         never rots into a stale snapshot. The name/email columns of such a row
         are only the fallback shown when the profile is gone. -->
    <addColumn tableName="EMAIL_CONTACT">
      <column name="PLATFORM_USERNAME" type="VARCHAR(250)"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-30">
    <!-- Where this provider's address book lives, so contacts can be read over
         CardDAV. It sits on the connector beside the IMAP and SMTP endpoints
         because it IS the same provider's third endpoint, not a different server:
         one preset per provider stays one thing for an administrator to maintain.
         Null means this provider offers no address book, which is the honest state
         for Microsoft 365 and Proton, and the default for everyone else. -->
    <addColumn tableName="EMAIL_CONNECTOR">
      <column name="CARDDAV_URL" type="NVARCHAR(500)"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-31">
    <!-- Who owns a contact's picture: USER when somebody set it by hand here,
         VCARD when the address-book sync wrote it. Without this the two are
         indistinguishable, and the sync would either overwrite a picture the user
         chose or never be able to refresh its own. Existing rows are all USER by
         definition, the sync not existing yet, and a null reads as USER. -->
    <addColumn tableName="EMAIL_CONTACT">
      <column name="PHOTO_ORIGIN" type="VARCHAR(10)"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-32" dbms="oracle,postgresql,hsqldb">
    <createSequence sequenceName="SEQ_EMAIL_CONTACT_ADDRESS_ID" startValue="1"/>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-33">
    <!-- A contact's addresses, one row each.
         Until now the address WAS the contact: EMAIL_CONTACT was unique on
         (USER_ID, PRIMARY_EMAIL), which made a person without an address
         unrepresentable. Mail always brought one, so it held; an address book does
         not. A Google address book of 496 entries produced 132 contacts because
         the rest carry a phone and no mail address, and those people are exactly
         the ones somebody wants to find in a contact list.
         So identity moves to the row, and addresses become what they are: zero or
         more attributes of a person. The unique index lives here now, which keeps
         de-duplication a database fact rather than a service promise, and keeps
         the lookup collection performs on every synced message a single indexed
         read. -->
    <createTable tableName="EMAIL_CONTACT_ADDRESS">
      <column name="ID" type="BIGINT" autoIncrement="${autoIncrement}" startWith="1">
        <constraints nullable="false" primaryKey="true" primaryKeyName="PK_EMAIL_CONTACT_ADDRESS"/>
      </column>
      <column name="CONTACT_ID" type="BIGINT">
        <constraints nullable="false"/>
      </column>
      <!-- Denormalised from the contact so uniqueness and lookup are one index on
           this table, without a join back on the hot path. -->
      <column name="USER_ID" type="VARCHAR(250)">
        <constraints nullable="false"/>
      </column>
      <column name="ADDRESS" type="VARCHAR(320)">
        <constraints nullable="false"/>
      </column>
    </createTable>
    <createIndex tableName="EMAIL_CONTACT_ADDRESS" indexName="UQ_EMAIL_CONTACT_ADDRESS" unique="true">
      <column name="USER_ID"/>
      <column name="ADDRESS"/>
    </createIndex>
    <createIndex tableName="EMAIL_CONTACT_ADDRESS" indexName="IDX_EMAIL_CONTACT_ADDRESS_CONTACT">
      <column name="CONTACT_ID"/>
    </createIndex>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-34">
    <!-- Every address already stored becomes a row of its own. The secondary ones
         live inside the EMAILS string and cannot be split in SQL; an upgrade plugin
         does those, and this handles the primary address, which is the one every
         lookup uses today. -->
    <sql>INSERT INTO EMAIL_CONTACT_ADDRESS (ID, CONTACT_ID, USER_ID, ADDRESS)
         SELECT ID, ID, USER_ID, PRIMARY_EMAIL FROM EMAIL_CONTACT WHERE PRIMARY_EMAIL IS NOT NULL</sql>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-35">
    <!-- The address is no longer the identity, so the contact stops being unique on
         it and stops requiring one at all. PRIMARY_EMAIL stays as the preferred
         address a contact is shown and written to by, which every list and the
         compose hand-off already read; it is now a display choice rather than a key,
         and it is null for a contact who has only a phone number. -->
    <dropIndex tableName="EMAIL_CONTACT" indexName="UQ_EMAIL_CONTACT_USER_EMAIL"/>
    <dropNotNullConstraint tableName="EMAIL_CONTACT" columnName="PRIMARY_EMAIL" columnDataType="VARCHAR(320)"/>
    <createIndex tableName="EMAIL_CONTACT" indexName="IDX_EMAIL_CONTACT_USER_EMAIL">
      <column name="USER_ID"/>
      <column name="PRIMARY_EMAIL"/>
    </createIndex>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-36">
    <!-- The rest of what a contact card should carry: birthday, postal address,
         note and website. These were lost twice before this: the CardDAV sync
         dropped them on read, and the export had nothing to write back.
         BIRTHDAY is text, not a DATE: vCard allows a year-less birthday
         (BDAY:- -0412) and a DATE column would force a fake year onto it. The
         canonical forms are YYYY-MM-DD and - -MM-DD.
         The address is STRUCTURED, one column per vCard ADR component, not one
         formatted string: the joined PHONES column is what made phone matching
         unreliable, and a flattened address could only ever be exported as one
         long street line. Five nullable columns paid once beats a parser nobody
         can write correctly later.
         NOTE_TEXT is a bounded NVARCHAR rather than a CLOB so the column stays
         cheap to read on every dialect; every write path caps it. -->
    <addColumn tableName="EMAIL_CONTACT">
      <column name="BIRTHDAY" type="VARCHAR(10)"/>
      <column name="ADDRESS_STREET" type="NVARCHAR(500)"/>
      <column name="ADDRESS_CITY" type="NVARCHAR(255)"/>
      <column name="ADDRESS_REGION" type="NVARCHAR(255)"/>
      <column name="ADDRESS_POSTAL_CODE" type="NVARCHAR(50)"/>
      <column name="ADDRESS_COUNTRY" type="NVARCHAR(255)"/>
      <column name="NOTE_TEXT" type="NVARCHAR(2000)"/>
      <column name="WEBSITE" type="NVARCHAR(500)"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-37">
    <comment>
      A note is written in the platform's rich editor now, so what is stored is
      markup rather than bare text and the same paragraph costs several times
      the characters. Still bounded rather than a CLOB, for the reason the
      original column gives.
    </comment>
    <modifyDataType tableName="EMAIL_CONTACT" columnName="NOTE_TEXT" newDataType="NVARCHAR(8000)"/>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-38" dbms="oracle,postgresql,hsqldb">
    <comment>
      Changeset 1.0.0-34 seeded EMAIL_CONTACT_ADDRESS with explicit ids (each row
      taking its contact's), but SEQ_EMAIL_CONTACT_ADDRESS_ID was created at 1 and
      hands out ids one at a time. So on every vendor that draws ids from that
      sequence, the first addresses written after the upgrade are handed ids the
      seeded rows already hold, and the insert fails on the primary key — until
      the sequence has climbed past the largest seeded id, one failure at a time.
      It is not a slow burn either: the contact address repair runs at the first
      startup after the upgrade and writes through the same sequence.
      MySQL is left out on purpose: its auto-increment counter advances itself
      when a row arrives with an explicit id, which is why the bug is invisible on
      the vendor most deployments run.
    </comment>
    <customChange class="org.exoplatform.emailConnector.upgrade.SequenceRepairChange">
      <param name="sequenceName" value="SEQ_EMAIL_CONTACT_ADDRESS_ID"/>
      <param name="tableName" value="EMAIL_CONTACT_ADDRESS"/>
    </customChange>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-39">
    <comment>
      Drafts. A draft is a row of EMAIL_BOX like any other message, with FOLDER =
      'DRAFTS' — not a table of its own. A conversation has to stay ONE query: the
      whole point of showing a draft is showing it at the bottom of the thread it
      belongs to, and a second table would mean every thread read became a join or
      a merge in Java, for the one row that matters most to the person reading.

      Four columns is all the difference amounts to, and all four are null on every
      row that is not a draft.

      DRAFT_LOCAL_ID is the handle the composer holds. It has to be OURS and it has
      to never change, because the only other candidate is the IMAP UID and that is
      not stable for a draft: IMAP cannot update a message, so saving a draft twice
      means appending a new message and deleting the old one, and the UID changes
      under the user mid-sentence. A UUID minted here survives that.

      DRAFT_STATE is where the row stands against the server copy (see DraftState):
      it is what lets a save that changed nothing skip re-uploading the entire
      message, attachments included.

      DRAFT_REVISION counts local edits. Autosaves are fired by a typing pause and
      travel over the network, so they can and do arrive out of order; the save
      path drops any that carries a revision the row has already passed. Without it
      a slow request carrying older text lands last and silently reverts the user's
      most recent sentence.

      DRAFT_UPDATED_DATE is the local edit time, kept apart from RECEIVED_DATE on
      purpose even though the two are equal today. RECEIVED_DATE is what every
      ORDER BY in this schema sorts on, and once drafts join the sync it will hold
      the INTERNALDATE of the copy on the server; the moment the user last typed is
      a different fact and it is the one the reader should show.
    </comment>
    <addColumn tableName="EMAIL_BOX">
      <column name="DRAFT_LOCAL_ID" type="NVARCHAR(64)"/>
      <column name="DRAFT_STATE" type="NVARCHAR(20)"/>
      <column name="DRAFT_REVISION" type="BIGINT"/>
      <column name="DRAFT_UPDATED_DATE" type="TIMESTAMP"/>
    </addColumn>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-40">
    <comment>
      Every draft read is "this user's draft with this local id" — the composer
      knows nothing else about the row. Not declared unique: uniqueness here would
      be enforced across a column that is null on every non-draft row, and vendors
      disagree about whether repeated nulls collide. The save path holds a per-draft
      lock instead, which is what actually serialises the writes.
    </comment>
    <createIndex tableName="EMAIL_BOX" indexName="IDX_EMAIL_BOX_DRAFT">
      <column name="USER_ID"/>
      <column name="DRAFT_LOCAL_ID"/>
    </createIndex>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-41">
    <comment>
      A draft that has not been uploaded has no IMAP UID, and MAIL_REMOTE_ID has
      been NOT NULL since 1.0.0-2. That was right for every row that had ever
      existed: each one mirrored a message on the server, and the UID was how it
      was addressed. A locally authored draft is the first row in this table that
      is not a copy of anything — it is written HERE and pushed up afterwards, so
      between the first keystroke and the first successful APPEND it has no UID to
      carry, which is the premise of the whole local-first design rather than an
      edge case. Slice 1 added the four draft columns and left this one alone, so
      the very first save of a new draft failed on the constraint.

      A sentinel UID (0, -1) was considered and rejected. The code reasons in terms
      of "a UID means a copy exists on the server" — see EmailBoxService's
      serverDraftCopyUid, which is what the push, the discard and the send all ask
      before removing a copy — so a sentinel would have to be excluded again at
      every one of those questions, and at the sync's UID-keyed lookups
      (findByMailRemoteIdAndUserIdAndFolder, findCachedMailRemoteIds), where every
      unsent draft of a mailbox would collide on the one sentinel value. It would
      also not even be sufficient: detachDraftFromServerCopy writes MAIL_REMOTE_ID
      = NULL when the copy a row pointed at is gone from the server, so the column
      has to accept NULL regardless — the sentinel would only move the same failure
      later, into the sync, where it is far harder to see.

      Dropping the constraint is additive and reversible: every existing row keeps
      its UID, and nothing that reads one starts having to handle a null it could
      not already get.
    </comment>
    <dropNotNullConstraint tableName="EMAIL_BOX" columnName="MAIL_REMOTE_ID" columnDataType="BIGINT"/>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-43" dbms="oracle,postgresql,hsqldb">
    <comment>
      The sequence EmailAttachmentEntity has been asking for since 1.0.0-5, and
      which was never created. Every other entity of this schema got one — 1.0.0-1
      for the connector, 1.0.0-3 for the mail row, 1.0.0-16 for the contact,
      1.0.0-33 for the contact address — and the attachment was simply missed.

      It is not cosmetic on the vendors listed here. The entity declares
      @GeneratedValue(AUTO) against a named @SequenceGenerator, so on every dialect
      that HAS sequences Hibernate draws the id from SEQ_EMAIL_ATTACHMENT_ID, and
      the insert fails outright: "user lacks privilege or object not found". Which
      means that on PostgreSQL and Oracle, caching a message that carries an
      attachment cannot have been storing the attachment row at all.

      MySQL is unaffected, and that is why this survived: it has no sequences, so
      Hibernate falls back to the table's own AUTO_INCREMENT and the ids come out
      fine. The same vendor asymmetry 1.0.0-38 documents for the contact-address
      sequence, from the other direction — it is the reason a defect in the id
      strategy is invisible on the vendor most deployments run.

      Numbered 43 rather than 42: 42 is taken by the body-format branch, which is
      stacked on the same drafts work and not merged either. Two branches landing on
      one number is a reconciliation nobody should have to do twice.

      Created starting at 1 rather than repaired past the existing rows, as 1.0.0-38
      had to: no row can exist here on these vendors, precisely because the sequence
      never did. On a database where rows DO exist (one migrated from MySQL), the
      SequenceRepairChange of 1.0.0-38 is the tool, and it is deliberately not run
      here — it would be one more thing to get right for a case nobody has.
    </comment>
    <createSequence sequenceName="SEQ_EMAIL_ATTACHMENT_ID" startValue="1"/>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-44">
    <comment>
      Attachments on drafts, changesets 44 through 47. On the numbering, since it has
      already cost one reconciliation: the drafts work this is stacked on holds 39 to
      41, the body-format branch — stacked on the same unmerged drafts work — holds
      42, and 43 is the missing attachment sequence this branch's previous commit had
      to add before an attachment row could be written at all. So this group starts at
      44. Anything landing after it starts at 48.

      ATTACHMENT_REMOTE_ID is a MIME part path — "2" is the second part of the
      message this row belongs to — and it has been NOT NULL since 1.0.0-5 because
      every attachment that had ever existed here was a part OF a received message,
      and the path was how it was addressed.

      A file the user attaches to a draft is the first one that is not. It is
      uploaded HERE, stored in the platform's file service, and only becomes a MIME
      part when the mail is finally sent — so between the paperclip and the Send
      button there is no part path to carry, which is the premise of the design
      rather than an edge case.

      Exactly the reasoning 1.0.0-41 applied to MAIL_REMOTE_ID one changeset group
      earlier, for the same feature and the same reason: a locally authored row is
      not a copy of anything, so the columns that address the thing it copies are
      empty until it becomes one.

      A sentinel path ("", "0") was considered and rejected on 1.0.0-41's grounds:
      the code asks "does this row name a part of a message" in several places, and
      a sentinel has to be excluded again at every one of them, while a null answers
      the question by being one.

      Dropping the constraint is additive and reversible: every existing row keeps
      its path, and nothing that reads one starts having to handle a null it could
      not already get — the attachment lookups all match ON the path, so a null one
      is simply never returned by them.
    </comment>
    <dropNotNullConstraint tableName="EMAIL_ATTACHMENTS" columnName="ATTACHMENT_REMOTE_ID" columnDataType="VARCHAR(250)"/>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-45">
    <comment>
      Where a draft's attachment actually lives. FILE_ID is the platform
      FileService id under the add-on's own namespace (EmailConnectorStorage
      NAME_SPACE), the same store contact photos and connector illustrations use —
      which is why nothing may ever sweep that namespace by "what the mail tables do
      not reference": it is shared, and the other two owners would lose their files.

      The bytes are deliberately NOT a column. A BLOB here would put every
      attachment of every draft into the row a conversation read joins, and the
      thread query already fetches this table; the file service exists precisely so
      binary content is addressed by id and read on demand.

      FILE_SIZE is denormalised from the file rather than joined for. The composer
      renders a size beside every chip and the send path caps a message's total, so
      it is read on every list and every send, and asking the file service per
      attachment for a number we already knew when we wrote it is a round trip per
      chip.

      The index is on FILE_ID alone, and it is not there for the read path — nothing
      looks an attachment up by its file. It is for the sweep (a later slice), which
      has to answer "is this file still referenced by any row" for each marker it
      considers, and would otherwise scan the table once per marker.
    </comment>
    <addColumn tableName="EMAIL_ATTACHMENTS">
      <column name="FILE_ID" type="BIGINT"/>
      <column name="FILE_SIZE" type="BIGINT"/>
    </addColumn>
    <createIndex tableName="EMAIL_ATTACHMENTS" indexName="IDX_EMAIL_ATTACHMENTS_FILE">
      <column name="FILE_ID"/>
    </createIndex>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-46">
    <comment>
      The orphan marker: a file that no row references any more, written down so
      that something can free it later.

      It exists because of exactly how mail rows are deleted. EmailBoxDAO's
      deleteEmailsByIds is a bulk JPQL DELETE — no JPA cascade, no entity
      callbacks, no loaded instances — and the attachment rows go with it through
      the DATABASE-level ON DELETE CASCADE declared in 1.0.0-6. Nothing in Java ever
      observes that happening. So there is no lifecycle hook to hang "and free the
      file" on: it has to be explicit, and it has to read the file ids BEFORE the
      rows are gone, because afterwards nothing anywhere knows which files those
      were.

      Two statements that cannot be one, therefore, and the order between them is
      the whole design. Recording first means a crash in between leaves a marker for
      a file that is still referenced — harmless, because the sweep verifies before
      it deletes. Deleting first would mean a crash leaves bytes in the file store
      that nothing names and nothing can ever find: a leak with no record of itself.
      A note nobody needed beats a file nobody can reach.

      USER_ID is carried so the sweep can be scoped and its work attributed; it is
      deliberately not a foreign key, since the point of the row is to outlive
      everything it refers to.

      FILE_ID is unique: recording the same file twice is a normal race (two
      cleanups over overlapping rows), and the second one should be a no-op rather
      than a second marker to process.

      This changeset only creates the table, and this slice only WRITES to it. The
      sweep that reads it is a later slice; until then the markers accumulate, which
      is the point — they are the record that the work is outstanding.
    </comment>
    <createTable tableName="EMAIL_ORPHAN_FILE">
      <column name="ID" type="BIGINT" autoIncrement="${autoIncrement}" startWith="1">
        <constraints nullable="false" primaryKey="true" primaryKeyName="PK_EMAIL_ORPHAN_FILE"/>
      </column>
      <column name="FILE_ID" type="BIGINT">
        <constraints nullable="false"/>
      </column>
      <column name="USER_ID" type="VARCHAR(250)"/>
      <column name="RECORDED_DATE" type="TIMESTAMP">
        <constraints nullable="false"/>
      </column>
    </createTable>
    <createIndex tableName="EMAIL_ORPHAN_FILE" indexName="UQ_EMAIL_ORPHAN_FILE" unique="true">
      <column name="FILE_ID"/>
    </createIndex>
    <modifySql dbms="mysql">
      <append value=" ENGINE=INNODB CHARSET=UTF8MB4 COLLATE utf8mb4_0900_ai_ci"/>
    </modifySql>
  </changeSet>
  <changeSet author="email-connector" id="1.0.0-47" dbms="oracle,postgresql,hsqldb">
    <comment>
      The orphan marker's sequence, on the vendors that have sequences — created in
      the same breath as its table, which is the lesson 1.0.0-43 paid for.
    </comment>
    <createSequence sequenceName="SEQ_EMAIL_ORPHAN_FILE_ID" startValue="1"/>
  </changeSet>

</databaseChangeLog>
